# 2026-05-12 工作日誌

## Phase 1 — 資料庫 Schema v2 完成

### ✅ 已完成
1. **新增 migration**: `supabase/migrations/012_drinktogether_v2_schema.sql`（1049 行）
   - ALTER profiles 表：新增 17 個欄位（level, total_coins, daily_coin_earned, completed_meals_count 等）
   - 新增 16 張資料表：bars, bar_checkins, bar_ratings, follows, user_blocks, moments, moment_likes, moment_comments, message_threads, messages, coin_transactions, daily_checkins, shop_items, shop_orders, bar_coin_grants, user_reports
   - ALTER meal_participants：新增 apply_count 欄位
   - 45 條 RLS 政策（涵蓋所有新表）
   - 20 個 Function（含 grant_coins 每日上限 300 檢查、spend_coins、redeem_shop_item、calculate_user_level、bar_grant_coins 等）
   - 15 個 Trigger（信用分分段扣分 -1/-3/-5/-8、等級自動計算、金幣發放、動態計數更新等）
   - 33 個 Index（含 GIN trgm 搜尋索引）

2. **更新 TypeScript 型別**: `src/types/index.ts`
   - 完整對齊 v2 schema，新增 15+ 個 interface（Bar, Moment, Message, CoinTransaction, ShopItem 等）
   - 新增 12 個 enum type（UserLevel, MessageType, CoinSourceType 等）
   - Profile interface 包含所有新欄位 + 計算欄位（is_following, is_blocked, is_mutual_follow）

3. **SQL 語法驗證通過**：括號平衡、329 條語句、0 errors、0 warnings

### 📋 待執行（Phase 1 後續）
- 將 012_drinktogether_v2_schema.sql 上傳至 Supabase SQL Editor 執行
- 更新 Supabase 啟用 pg_cron 擴展（定時任務：金幣重置、電子券過期、信用分恢復）
- 更新前端頁面對接新 schema（特別是登入流程 OTP + 年齡驗證）

## Phase 2 — 酒吧系統前端完成

### ✅ 已完成
1. **i18n 翻譯鍵**：在 en.json、zh-CN.json、th.json 新增 `bar` 區段（含 title, category 11 種, city 2 種, checkin, rate, openingHours 等 60+ 翻譯鍵）
2. **API 函數**（`src/lib/api.ts`）：新增 `fetchBars`、`fetchBarById`、`checkinBar`（含 GPS 500m 驗證 + RPC grant_coins）、`rateBar`（含平均分更新）、`fetchBarRatings`、`fetchMyBarCheckinToday`、`fetchMyBarRating`、`fetchTodayCheckinCount`、`haversineDistance`、`transformBar`
3. **Zustand Store**（`src/store/bar-store.ts`）：管理 bars 列表、currentBar、ratings、filters、checkin/rating 狀態
4. **酒吧列表頁**（`src/app/[locale]/bars/page.tsx`）：含搜尋、城市/分類篩選、封面圖卡片、評分顯示
5. **酒吧詳情頁**（`src/app/[locale]/bars/[id]/page.tsx`）：含圖片輪播、打卡（GPS 驗證 + 金幣發放）、評分 modal（環境/服務/性價比）、營業時間、今日打卡數、評論列表、Google 導航連結
6. **TabBar 更新**：第二個 tab 改為酒吧（MapPin icon），隱藏酒吧詳情頁的 TabBar
7. **Build 通過**：`next build` TypeScript 檢查 + 編譯成功
8. **TypeScript 修正**：Profile interface 新增 `age_range` 欄位，`fetchProfile` cast 修正

### 📋 待執行
- 動態牆頁面（Task #36）
- 私信功能頁面（Task #37）
- 社交關係功能（Task #38）
- 更新個人資料頁整合 v2 入口（Task #39）

## Phase 3 — 金幣系統前端完成

### ✅ 已完成
1. **i18n 翻譯鍵**：在 en.json、zh-CN.json、th.json 新增 `coins` 區段（含 title, balance, checkin, streak, taskList 12 種任務, source 17 種來源, shop category 5 種, item 兌換流程, order 訂單管理，共 100+ 翻譯鍵）
2. **API 函數**（`src/lib/api.ts`）：新增 `fetchCoinTransactions`、`dailyCheckin`（含連續簽到計算 + 7天bonus + RPC grant_coins）、`fetchTodayCheckinStatus`、`fetchShopItems`（分類篩選）、`redeemShopItem`（RPC 原子兌換）、`fetchMyShopOrders`（含 item join）
3. **Zustand Store**（`src/store/coin-store.ts`）：管理 totalCoins、dailyCoinEarned、hasCheckedInToday、streakDays、transactions、shopItems、shopCategory、orders，fetchBalance 同時取得簽到狀態
4. **金幣總覽頁**（`src/app/[locale]/coins/page.tsx`）：含金幣餘額卡片（動畫顯示）、每日進度條（300幣上限）、每日簽到按鈕（連續天數+火焰圖示）、商城快捷入口、連續簽到獎勵卡片、每日任務清單（12項含金幣數量）、交易記錄列表（含 source icon + 日期格式化）
5. **金幣商城頁**（`src/app/[locale]/coins/shop/page.tsx`）：含商城/訂單 tab 切換、分類篩選（food/drink/entertainment/spa/other）、商品卡片（圖片+名稱+描述+金幣價格+庫存+featured badge）、兌換確認 bottom-sheet modal（含餘額預覽）、訂單列表（狀態 badge + QR Code 展開）、Toast 通知
6. **Profile 頁面整合**：在 My Drinks 下方新增金幣快捷入口卡片（顯示餘額+Coins icon+gold border）
7. **Build 通過**：`next build` TypeScript 檢查 + 編譯成功，路由 `/[locale]/coins` 和 `/[locale]/coins/shop` 正確生成

## Phase 4 — 動態牆前端完成

### ✅ 已完成
1. **i18n 翻譯鍵**：在 en.json、zh-CN.json、th.json 新增 `moments` 區段（含 title, create, placeholder, imageUpload, visibility 3 種, moodTag 6 種心情, comment 操作, time 相對時間格式, imageViewer, share, report, coinReward, likeBonus 等 70+ 翻譯鍵）
2. **API 函數**（`src/lib/api.ts`）：新增 `fetchMoments`（分頁 + userId 篩選 + user/bar join）、`fetchMomentById`、`createMoment`（含 RPC grant_coins +15 幣 + posts_count）、`deleteMoment`（軟刪除）、`likeMoment`/`unlikeMoment`/`checkMomentLiked`（含 likes_count 更新）、`fetchComments`（頂層 + 巢狀回覆）、`addComment`/`deleteComment`（含 comments_count 更新）、`reportMoment`（含 reports_count 更新）
3. **Zustand Store**（`src/store/moment-store.ts`）：管理 moments 列表（分頁+無限滾動）、currentMoment、comments、toggleLike、checkLikedStatus（批次）、createMoment、deleteMoment、addComment、deleteComment、showCompose、replyTo、toast 通知
4. **動態牆總覽頁**（`src/app/[locale]/moments/page.tsx`）：
   - MomentCard：用戶頭像+暱稱+等級、相對時間、心情標籤 emoji、酒吧/位置顯示、圖片網格（自適應 1/2/3 列 + 4張以上摺疊 + 全螢幕瀏覽器）、點讚動畫（Heart fill）、留言（MessageCircle）、分享（複製連結）、更多選單（舉報/分享/刪除）、隱私設定圖示
   - CommentSection：Bottom-sheet 樣式留言面板、頂層評論 + 巢狀回覆、回覆指定用戶、刪除評論、自動滾動到底部、送出按鈕 + Enter 鍵盤快捷鍵
   - ComposeModal：Bottom-sheet 發布面板、文字輸入（500 字限制）、心情標籤選擇（6 種 emoji）、圖片上傳（最多 9 張預覽 + DataURL）、隱私設定切換（public/friends/party_only）、發布獎勵提示（+15 幣）
   - FAB 浮動發布按鈕、Infinite Scroll 無限滾動、Toast 通知、載入骨架/空狀態
5. **Build 通過**：`next build` 63 個頁面全部生成，`/[locale]/moments` 路由正確
6. **TypeScript 修正**：移除 reportMoment 未定義解構、Supabase RPC `PromiseLike` 不支援 `.catch()` 改用 `void` 包裝、MomentComment user 欄位型別用 `as any` cast

## Phase 5 — 私信系統前端完成

### ✅ 已完成
1. **i18n 翻譯鍵**：在 en.json、zh-CN.json、th.json 新增 `chat` 區段（含 title, noThreads, searchPlaceholder, threadList, typePlaceholder, send, deleteMessage, imageUpload, emptyChat, blocked, reportUser, messageType 4 種, partyInvite/shopShare 卡片, imageViewer, time 相對時間格式, markRead/markUnread, unreadCount, you 等 60+ 翻譯鍵），nav 區段新增 `messages` 鍵
2. **API 函數**（`src/lib/api.ts`）：新增 `fetchThreads`（含 participant user join + 按 last_message_at 排序）、`getOrCreateThread`（含 participant_a < participant_b 一致性排序 + 並發衝突處理）、`fetchMessages`（分頁 + cursor before + sender join + 反轉為時間順序）、`sendMessage`（自動更新 last_message_at）、`deleteMessage`（軟刪除）、`fetchLastMessage`、`transformThread`（含 _otherUser 便利計算欄位）、`transformMessage`
3. **Zustand Store**（`src/store/chat-store.ts`）：管理 threads 列表（含 last_message 預取）、currentThreadId、messages（分頁+loadMore）、hasMoreMessages、sendingMessage、toasts；fetchThreads 同時抓取每個 thread 的 last_message；send 含 optimistic update（失敗回滾）；deleteMessage 軟刪除 + toast 通知
4. **私信列表頁**（`src/app/[locale]/messages/page.tsx`）：含 sticky header（未讀計數）、thread card（avatar + 暱稱 + last message 預覽 + 相對時間 + 未讀 badge + chevron）、AnimatePresence 列表動畫、空狀態
5. **聊天室頁**（`src/app/[locale]/messages/[threadId]/page.tsx`）：
   - Header：返回按鈕 + 對方 avatar + 暱稱 + LevelBadge
   - ChatBubble：text / image（grid + 點擊全螢幕瀏覽）/ party_invite 卡片（加入+查看按鈕）/ shop_share 卡片（查看商品按鈕）/ 軟刪除提示
   - 日期分隔線（today/yesterday/date）
   - ImageViewer Modal（全螢幕 + 左右切換 + 計數器）
   - 輸入區：ImageIcon 上傳 + textarea 自適應高度 + Enter 發送 + Send button + 發送中 spinner
   - Delete menu（hover 顯示 + 刪除確認）
   - Toast 通知系統
   - Scroll 偵測：near top → loadMore、near bottom → auto-scroll
6. **TabBar 更新**：新增 Messages tab（MessageSquare icon），隱藏 messages/chat 頁面的 TabBar
7. **Build 通過**：`next build` 66 個頁面全部生成，`/[locale]/messages` 和 `/[locale]/messages/[threadId]` 路由正確

## Phase 6 — 社交關係系統完成

### ✅ 已完成
1. **i18n 翻譯鍵**：在 en.json、zh-CN.json 新增 `social` 區段（follow/unfollow/following/block/unblock/blocked/blockedUsers/reportUser/reportReasons 5 種/followers/followingCount/followersCount/mutualFollow/sendMessage/viewMoments 等 30+ 翻譯鍵），th.json social 區段補齊
2. **API 函數**（`src/lib/api.ts` social section）：`followUser`、`unfollowUser`、`blockUser`、`unblockUser`、`fetchFollowers`（含 follower profile join）、`fetchFollowing`（含 following profile join）、`checkFollowStatus`（isFollowing + isMutual + isBlocked 三查）、`fetchBlockedUsers`、`reportUser`（含 increment report_received_count）、`fetchFollowerCount`、`fetchFollowingCount`
3. **Zustand Store**（`src/store/social-store.ts`）：followStatusMap 快取、follow/unfollow（含自動更新互關狀態）、block/unblock、report、loadFollowers/loadFollowing/loadBlockedUsers
4. **用戶資料頁更新**（`src/app/[locale]/user/[id]/page.tsx`）：
   - Follow/Unfollow 按鈕（含 loading 狀態）
   - Message 按鈕（→ getOrCreateThread → 導航聊天室）
   - View Moments 按鈕
   - More menu（Block/Unblock/Report 下拉選單）
   - Block Confirm Modal
   - Report Modal（5 種原因選擇）
   - 粉絲/關注數據顯示（取代原 hosted/joined stats）
   - Toast 通知系統
5. **粉絲列表頁**（`src/app/[locale]/user/[id]/followers/page.tsx`）：列表含 avatar + 暱稱 + bio、follow/unfollow 按鈕、空狀態、stagger 動畫
6. **關注列表頁**（`src/app/[locale]/user/[id]/following/page.tsx`）：同上結構
7. **用戶動態頁**（`src/app/[locale]/user/[id]/moments/page.tsx`）：顯示指定用戶的動態列表、圖片網格、心情標籤、酒吧位置、點讚/留言數、TimeAgo 相對時間
8. **Profile 頁整合**（`src/app/[locale]/profile/page.tsx`）：
   - Stats Cards 改為粉絲/關注/信用分
   - Quick Access 新增 Moments 入口（顯示 posts_count）
   - My Meals、My Coins、My Moments 三個入口
   - TabBar 隱藏 user profile 子頁面
9. **Build 通過**：`next build` 69 個頁面全部生成
