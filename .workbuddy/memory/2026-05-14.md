# 2026-05-14 工作日誌

## Drink Together v2 DB Migration 全部上線
- ✅ Batch 1（ALTER profiles + CREATE 16 新表）：已完成
- ✅ Batch 2（RLS Policies 45 條）：修正 `CREATE POLICY IF NOT EXISTS` 語法為 `DROP + CREATE` 後執行成功
- ✅ Batch 3（Functions & Triggers）：20 Functions + 15 Triggers 全部執行成功
- ✅ pg_cron 已啟用並排程完成：`reset-daily-coins`（每日 0:00 UTC）、`monthly-credit-recovery`（每月 1 日）、`expire-shop-orders`（每小時）

## 前端 api.ts 修復（對齊 v2 DB trigger）
- ✅ 修復 `dailyCheckin` 金幣重複發放：移除前端手動 `grant_coins` RPC，改由 DB `on_daily_checkin` BEFORE INSERT trigger 自動處理
- ✅ 修復 `checkinBar` 金幣重複發放：移除前端手動 `grant_coins` RPC，改由 DB `on_bar_checkin` AFTER INSERT trigger 自動處理
- ✅ 移除 5 個不存在的舊 RPC 呼叫：`increment_posts_count`（trigger on_moment_created）、`increment_moment_likes`（trigger on_moment_liked）、`increment_moment_comments`×2（trigger on_moment_commented）、`increment_moment_reports`、`increment_user_reports`（trigger on_report_resolved）
- ✅ Build 69 頁面全部通過
- ⚠️ Vercel CLI token 過期，需手動部署（GitHub push 或重新登入 Vercel CLI）
